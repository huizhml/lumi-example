name: py3
channels:
  - conda-forge
  - defaults
dependencies: # keep few libraries here, otherwise it won't finish
  - python==3.11
  - libgdal-jp2openjpeg
  - pip==23.2.1
  - pip:
      - jsonschema
      - matplotlib
      - numpy
      - scikit-learn
      - scipy